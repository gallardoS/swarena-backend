# Keep the generated context smoke test independent from a running MySQL instance.
# Database behavior will be covered by dedicated repository/integration tests.
spring.autoconfigure.exclude=org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration
management.health.db.enabled=false
